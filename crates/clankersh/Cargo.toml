[package]
name = "clankersh"
version = "0.1.0"
edition = "2021"

[dependencies]
# Workspace crates
omega-sh-client = { path = "../omega-sh-client" }

# Async runtime
tokio = { version = "1.0", features = ["full"] }

# Serialization
serde_json = "1.0"

# Error handling
anyhow = "1.0"
