[package]
name = "omega-protocol"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"
description = "Canonical wire types shared by omega's Unix-socket processes"

[dependencies]
omega-core = { path = "../omega-core" }
omega-projects = { path = "../omega-projects" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
